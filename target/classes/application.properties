# Temporary fix for schema issues 
spring.sql.init.mode=never 
spring.jpa.hibernate.ddl-auto=create-drop 
spring.h2.console.enabled=true 
spring.datasource.url=jdbc:h2:mem:testdb 
spring.datasource.username=sa 
spring.datasource.password= 

# Fix for bean definition override issues
spring.main.allow-bean-definition-overriding=true

# Server configuration
server.port=8088

# Firebase configuration (service account file exists)
firebase.bucket-name=sep490-e5896.appspot.com

# Email service configuration (disabled for now)
email.service.enabled=false
